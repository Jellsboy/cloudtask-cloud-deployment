# Server configuration
PORT=3000
APP_NAME=CloudTask
NODE_ENV=production

# Comma-separated origins if API is exposed separately.
# Leave empty when frontend and API use the same domain.
ALLOWED_ORIGINS=
